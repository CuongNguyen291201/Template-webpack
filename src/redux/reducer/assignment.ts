import { Reducer } from "redux";
const initialState: any = {
}
const assignmentReducer: Reducer<any, any> = (state: any = initialState, action: any): any => {
    return state;
}

export { assignmentReducer };
